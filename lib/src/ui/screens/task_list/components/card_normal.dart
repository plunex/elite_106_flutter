import 'package:flutter/material.dart';

class CardNormal extends StatelessWidget {
  const CardNormal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Sample"),
    );
  }
}
